# Docker Environment Configuration Example
# Copy this file to .env.docker and update the values

# MongoDB Configuration
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=your-secure-mongo-password-here
MONGO_DATABASE=chatNest

# Mongo Express Configuration (Database Admin UI)
MONGO_EXPRESS_USERNAME=admin
MONGO_EXPRESS_PASSWORD=your-secure-admin-password-here

# Port Configuration
CLIENT_PORT=3000
SERVER_PORT=5001
MONGO_PORT=27017
MONGO_EXPRESS_PORT=8081

# Security Notes:
# 1. Change all passwords before using in production
# 2. Use strong, unique passwords (12+ characters with mixed case, numbers, symbols)
# 3. Never commit the actual .env.docker file to version control
# 4. For production, consider using Docker secrets instead of environment variables

# Example of strong passwords:
# MONGO_INITDB_ROOT_PASSWORD=MyS3cur3M0ng0P@ssw0rd!2024
# MONGO_EXPRESS_PASSWORD=Adm1nP@ssw0rd!Secure2024
